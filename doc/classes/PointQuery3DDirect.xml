<?xml version="1.0" encoding="utf-8" ?>
<class name="PointQuery3DDirect" inherits="RefCounted" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../class.xsd">
	<brief_description>
		A query for a specific point in space.
	</brief_description>
	<description>
		[admonition type="note" collapsible="true" open="false"]
		[b]Part of [i]Godot Engine: Conservatory Edition[/i].[/b]
		This is not available in the official build of Godot.
		[/admonition]
		This class queries for physics bodies and areas a specific point.
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="query" qualifiers="const">
			<return type="bool" />
			<param index="0" name="space" type="RID" />
			<param index="1" name="max_count" type="int" />
			<description>
				Performs a query using the properties of this object as parameters. Can return up to [param max_count] items.
			</description>
		</method>
		<method name="query_statically">
			<return type="bool" />
			<param index="0" name="space" type="RID" />
			<param index="1" name="parameters" type="PhysicsPointQueryParameters3D"/>
			<param index="2" name="max_count" type="int" />
			<description>
				Statically perform a query using the provided [param parameters]. Can return up to [param max_count] items.
			</description>
		</method>
		<method name="query_statically_preallocated">
			<return type="int" />
			<param index="0" name="space" type="RID" />
			<param index="1" name="parameters" type="PhysicsPointQueryParameters3D"/>
			<param index="2" name="result_preallocated" type="Array" />
			<description>
				Statically perform a query using the provided [param parameters]. This variation of the method is special in that it expects the passed array ([param result_preallocated]) to have its size set [b]before[/b] calling this method. The size of this array is used to determine the number of results.
				This variation of the method is most optimal, as any instances of results in the array will be overwritten, instead of an entirely new result object being created.
				The array passed in is obviously edited; it is your result array. The return value is the amount of results actually placed into the array.
			</description>
		</method>
	</methods>
	<members>
		<member name="collide_with_areas" type="bool" setter="set_collide_with_areas" getter="is_collide_with_areas_enabled" default="false">
			If [code]true[/code], collisions with physics areas (like [Area3D]) will be reported.
		</member>
		<member name="collide_with_bodies" type="bool" setter="set_collide_with_bodies" getter="is_collide_with_bodies_enabled" default="true">
			If [code]true[/code], collisions with physics bodies (like [PhysicsBody3D]) will be reported.
		</member>
		<member name="collision_mask" type="int" setter="set_collision_mask" getter="get_collision_mask" default="1">
			The query's collision mask. Only objects in at least one collision layer enabled in the mask will be detected. See [url=$DOCS_URL/tutorials/physics/physics_introduction.html#collision-layers-and-masks]Collision layers and masks[/url] in the documentation for more information.
		</member>
		<member name="position" type="Vector3" setter="set_position" getter="get_position">
			The position in space where this query will occur.
		</member>
		<member name="exclude" type="Array" setter="set_exclude" getter="get_exclude">
			The RIDs in this array will be excluded from the query and cannot be hit.
		</member>
	</members>
</class>
